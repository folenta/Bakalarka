"""if isinstance(angle, str):
            if len(angle_difference) >= 2:
                angle_points = points[-2:]

                x1, y1 = get_coordinates(angle_points[0])
                x2, y2 = get_coordinates(angle_points[1])

                angle = math.atan2(float(y2) - float(y1), float(x2) - float(x1))
                angle = math.degrees(angle)"""

        #if len(angle_difference) isinstance(angle_difference[-1], float):
            #angle = math.radians(angle - angle_difference)

        #if True:
            """angle_difference = angle_difference.split(' ')
            angles = angle_difference[-1]
            if isinstance(angles, str) or len(angle_difference) == 1:
                angle = math.radians(angle)
            else:
                #angle_difference = float(angles)
                angle_difference = get_average_angle_difference()
                angle = math.radians(angle - angle_difference)"""

        #angle_difference = get_average_angle_difference(angle_difference, points)
        #angle = math.radians(angle - angle_difference)
        """if len(angle_difference) > 2:
            angle_diff = get_average_angle_difference(angle_difference, points)
            angle_difference[:] = []
            angle_difference.append(angle_diff)
        else:
            angle_diff = angle_difference[0]

        if angle < 0:
            angle = math.radians(angle + angle_diff)
        else:
            angle = math.radians(angle - angle_diff)"""

